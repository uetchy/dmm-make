require 'spec_helper'

describe Dmm::Make do
  it 'has a version number' do
    expect(Dmm::Make::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
